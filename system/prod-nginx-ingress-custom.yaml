---

controller:
  nodeSelector:
    beta.kubernetes.io/os: linux
  replicaCount: 2
  metrics:
    enabled: "true"
  stats:
    enabled: "true"
  service:
    externalTrafficPolicy: "Local"
    loadBalancerIP: 51.124.93.192
defaultBackend:
  nodeSelector:
    beta.kubernetes.io/os: linux
rbac:
  create: "true"
