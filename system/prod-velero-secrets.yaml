apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: velero
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-kv-secret-store
  target:
    name: velero-azure-kv
  dataFrom:
    - extract:
        key: k8s-secrets-velero-02
